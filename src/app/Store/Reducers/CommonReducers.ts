// import { SELECT_COUNTRY, SELECT_LANG } from "../Actions/CommonActions";

const initialState = {
  data: {},
};

const selectLanguageReducer = (state = initialState, action:any) => {
    // switch (action.type) {
    //     case SELECT_LANG:
    //       return {
    //         state,
    //       };
    //     default:
    //       return state;
    //   }
};
export default  selectLanguageReducer;